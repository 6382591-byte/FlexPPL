const test=require("node:test"),assert=require("node:assert/strict"),progress=require("../js/progress.js");
const history=[{id:"w2",name:"Pull A",date:"2026-07-10",items:[{exerciseId:"barbell-curl",name:"Barbell Curl",weight:65,sets:[10,10,9],done:true,coach:"REPEAT",next:65},{exerciseId:"barbell-row",name:"Barbell Bent-Over Row",weight:135,sets:[10,10,10],done:true,coach:"INCREASE",next:140},{exerciseId:"walking-lunge",name:"Walking Lunge",weight:25,sets:[12,12,12],done:false,skipped:true}],core:[{exerciseId:"plank",name:"Plank",target:45,actual:45,completed:true}]},{id:"w1",name:"Pull A",date:"2026-07-03",items:[{exerciseId:"barbell-curl",name:"Barbell Curl",weight:60,sets:[10,10,10],done:true,coach:"INCREASE",next:65},{exerciseId:"barbell-row",name:"Barbell Bent-Over Row",weight:125,sets:[10,10,10],done:true,coach:"INCREASE",next:135}],core:[]}];
test("progress is summary-first and grouped by explicit muscle categories",()=>{const result=progress.buildProgress(history,[]);assert.equal(result.summary.workouts,2);assert.ok(result.summary.totalVolume>0);assert.deepEqual(result.groups.map(group=>group.category),["Back","Biceps","Core"]);assert.ok(!result.entries.some(entry=>entry.name==="Walking Lunge"))});
test("canonical IDs keep sessions together",()=>{const biceps=progress.groupByCategory(history).find(group=>group.category==="Biceps");assert.equal(biceps.exercises.length,1);assert.equal(biceps.exercises[0].sessions.length,2)});
test("PRs use completed data and ignore zero-weight core weight records",()=>{const prs=progress.personalRecords(history);assert.ok(prs.some(record=>record.name==="Barbell Curl"&&record.type==="weight"&&record.value===65));assert.ok(!prs.some(record=>record.name==="Plank"&&record.type==="weight"))});
test("bodyweight summary stays compact",()=>{const result=progress.bodyweightSummary([{value:180.2,date:"2026-07-10"},{value:181,date:"2026-07-03"}]);assert.equal(result.latest,180.2);assert.equal(result.change,-0.8)});
test("exercise details are compact",()=>{const details=progress.exerciseDetails(history,"barbell-curl");assert.equal(details.category,"Biceps");assert.equal(details.sessions.length,2);assert.equal(details.bestWeight,65)});
